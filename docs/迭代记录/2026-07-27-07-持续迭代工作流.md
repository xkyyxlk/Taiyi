# 迭代-0007：持续迭代工作流

## 基本信息

- 时间：2026-07-27 15:26:30 +08:00
- 版本或里程碑：`v0.1.1` 准备
- 工作项：流程建设，尚未分配 `TY` 编号
- 状态：已完成
- 记录性质：历史回填

## 目标

- 建立适合持续开源迭代的工作流，使新开发者或新 Codex 会话可以恢复未完成工作。

## 本次完成内容

- 建立项目迭代工作流、当前迭代状态和会话交接模板。
- 规定工作项编号、状态流转、分支、验证、审核、发布和多会话协作方式。
- 新增迭代任务议题模板，并完善缺陷、功能和合并请求模板。
- 新增项目治理自动测试，并在项目说明、贡献指南和协作约束中接入新流程。

## 重要决策

- 使用 `TY-<数字>` 作为工作项编号，Codex 分支采用 `codex/ty-<编号>-<主题>`。
- 当前断点必须写入仓库，不得依赖聊天记录；未完成工作优先形成可恢复的检查点提交。
- 架构、数据结构、身份不变量、归一协议和安全边界变化必须记录架构决策。

## 验证结果

- `ruff format --check .`：通过。
- `ruff check .`：通过。
- `mypy src/taiyi`：通过。
- `pytest`：通过，共十九项测试。
- Markdown 中文规范与本地链接检查：通过。

## 相关提交

- `f31b828d663c6ef697181afab20ee3db30182e7f`：建立持续迭代与会话交接工作流。

## 影响范围

- 仓库协作规范、文档入口、GitHub 模板、治理测试和新会话恢复流程。

## 遗留事项

- 当前状态文件会滚动更新，Git 历史又不便按迭代阅读；仍需建立只追加的永久迭代档案。
