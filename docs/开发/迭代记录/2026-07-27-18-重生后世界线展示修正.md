# 迭代-0018：重生后世界线展示修正

## 基本信息

- 时间：2026-07-27 18:38 +08:00
- 版本或里程碑：`v0.1.1` 产品原型反馈准备
- 工作项：`TY-202607271832` 产品原型重生后世界线展示修正
- 状态：已完成
- 记录性质：会话迭代

## 目标

- 修正重生第三个同位体后“3/2 已就绪”的数量表达；
- 区分前两个同源比较对象和从归一后快照派生的下一代；
- 保持真实基础快照、既有比较对象和领域不变量不变。

## 本次完成内容

- 将待编号反馈草案转正为 `TY-202607271832` 并创建规范功能分支；
- 将前两个同位体显示为“本次比较组”，继续作为既有差异与归一提案的对象；
- 将第三个及后续重生实例单独显示为“下一代同位体”；
- 零至一个同位体继续显示核心比较进度，两个时显示“比较组已就绪”，超过两个时显示
  实际派生总数；
- 在两个分组中继续显示每个同位体的真实基础快照和世界线编号；
- 增加下一代分组的轻量视觉区分，不改变整体信息架构；
- 扩展原型集成测试，断言前两个同位体同源、协调者来自新快照，并检查静态页面文案；
- 使用全新数据目录再次完成创建、分叉、经历、比较、人工综合、应用、重生和回滚流程。

## 重要决策

- 继续沿用现有创建顺序：前两个同位体是当前核心比较组，后续实例是下一代展示组；
- 不为纯展示修正增加状态投影字段或改变内部 JSON；
- 不重新计算或替换已经应用的比较提案，历史比较对象保持只读；
- 回滚当前指针后，下一代仍显示其创建时的真实基础快照，不根据当前指针重新归类。

## 验证结果

- 修改前基线 `pytest tests/integration/test_prototype.py`：通过，三项测试；
- `node --check src/taiyi/prototype/static/app.js`：通过；
- 修改后 `pytest tests/integration/test_prototype.py`：通过，三项测试；
- 首次完整检查：格式、Ruff 和 Mypy 通过；一项静态资源断言因错误禁止合法的 `0/2`、
  `1/2` 表达而失败，收窄为只禁止旧的无条件模板后重新验证；
- `.venv/Scripts/ruff.exe format --check .`：通过，共检查九十个文件；
- `.venv/Scripts/ruff.exe check .`：通过；
- `.venv/Scripts/mypy.exe src/taiyi`：通过，共检查三十二个源文件；
- `.venv/Scripts/pytest.exe`：通过，共二十五项测试；
- `git diff --check`：通过；
- 全新数据目录的本地浏览器核心流程：通过；
- 重生后显示“已派生 3 个”、比较组与下一代分组及三个真实基础快照：通过；
- 回滚后分组和创建时基础快照保持不变：通过；
- 浏览器控制台错误和警告：无。

## 相关提交

- `9a0cd7c`：`TY-202607271832 流程：转正世界线展示修正`；
- `dce4ada`：`TY-202607271832 修复：区分比较组与下一代同位体`；
- 本记录和最终交接状态处于后续同一提交，通过本文件的 Git 历史定位。

## 影响范围

- 修改产品原型静态页面的同位体分组、计数文案和样式；
- 扩展现有原型集成测试并更新未发布变更日志；
- 不修改领域模型、数据库、迁移、内部 JSON、命令行接口或导出格式；
- 不改变世界线隔离、人工审核门、快照不可变、陈旧提案和回滚语义。

## 遗留事项

- 功能分支尚未推送或合入 `main`；
- `TY-202607271832` 的外部正式议题仍待补录；
- 仍需少量早期探索者反馈验证分组文案是否足够直观。

## 更正说明
