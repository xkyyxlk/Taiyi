# 迭代-0023：Agent 记忆分析方向草案

## 基本信息

- 记录编号：`迭代-0023`
- 时间：2026-07-28 16:08 +08:00
- 版本或里程碑：`v0.1.1` 发布后的方向规划
- 工作项：尚无正式工作项；完整范围已进入待编号草案
- 状态：已完成
- 记录性质：会话迭代

## 目标

- 压力测试现有身份演化产品原型的现实问题、目标用户、产品意义和未来方向；
- 收敛下一阶段的产品定位、首个场景、实现边界、模拟方法和量化验收原则；
- 在不编造正式编号、不创建功能分支和不修改产品代码的前提下形成可追溯草案。

## 本次完成内容

- 确认项目当前工程闭环成立，但个人 AI、研究工具和 Agent 基础设施三条方向不能同时推进；
- 将首批用户收敛为需要审计、比较和治理长期记忆的 Agent 开发者与研究型开发者；
- 将产品中心从身份版本合并调整为 Agent 长期记忆分析与变更治理；
- 确认第一版采用本地优先、只读、旁路的 CLI/CI 工具，不替换外部记忆库；
- 确认 LangGraph/LangSmith 参考适配器与框架无关 JSONL 协议；
- 确认显式来源、稳定记忆 ID、作用域、版本化协议和结果降级规则；
- 确认首个里程碑只做确定性分析，大模型语义裁决推迟；
- 确认大规模模拟实验先于真实项目验证，并建立初始规模、方法和退出原则；
- 确认单维护者 4–6 周的建议执行顺序和性能预算冻结方式；
- 新建待编号工作项草案并登记草案索引。

## 重要决策

- 版本、差异和回滚保留为底层机制，不再作为产品价值本身；
- 旧身份演化原型进入兼容维护，新方向使用独立中性领域模型和存储；
- 确定性错误可以阻断 CI，语义风险默认告警，模型不得自动裁定事实真假；
- 模拟标准答案由确定性规则生成，模型只能增加输入多样性；
- 模拟验证工程正确性，不能替代后续真实项目的价值验证；
- 在分析核心完成后建立性能基线，再冻结预算并限制无依据回退。

## 验证结果

- `git status --short --branch`：修改前通过，`main` 与 `origin/main` 同步且工作树干净；
- `git diff --check`：通过；
- 新增、修改 Markdown 相对链接检查：通过；
- 项目治理与中文规范测试：通过。

## 相关提交

- 当前记录与草案处于同一后续提交，通过本记录的文件历史定位。

## 影响范围

- 新增一份待编号工作项草案和本次永久迭代记录；
- 更新草案索引、迭代记录索引和当前迭代状态；
- 不修改产品代码、数据库、接口、现有产品定义、架构决策或外部状态。

## 遗留事项

- 维护者尚未分配正式 `TY-<数字>` 编号，也未建立外部议题；
- 取得正式编号后才能创建功能分支、更新产品定义和架构决策记录并开始实现；
- 协议字段、规则编号、CLI、性能绝对预算和参考适配器版本仍需在正式工作项中细化。

## 更正说明

- 无。
