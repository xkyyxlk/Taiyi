# 迭代-0008：迭代历史归档

## 基本信息

- 时间：2026-07-27
- 版本或里程碑：`v0.1.1` 准备
- 工作项：流程增强，尚未分配 `TY` 编号
- 状态：已完成
- 记录性质：会话迭代

## 目标

- 建立不会被当前状态覆盖的永久迭代档案，让维护者能逐次查看目标、改动、决策、验证、提交和遗留事项。

## 本次完成内容

- 新增只追加的迭代记录目录、索引和标准模板。
- 根据 Git 提交与仓库内容，为此前七次迭代回填中文历史记录。
- 将归档要求接入协作约束、迭代工作流、会话交接、项目入口和自动测试。
- 明确每次产生仓库改动的开发迭代都要建档，跨会话工作项可以保留多份会话记录。

## 重要决策

- `docs/开发/迭代状态.md` 只描述当前现场；`docs/开发/迭代记录/` 永久保存每次迭代，两者职责分离。
- 已完成记录只追加、不删除、不覆写；发现错误时在原文件末尾追加更正说明。
- 变更日志面向使用者，不替代包含工程细节和验证证据的迭代记录。

## 验证结果

- `.venv/Scripts/ruff.exe format --check .`：通过，共检查七十个文件。
- `.venv/Scripts/ruff.exe check .`：通过。
- `.venv/Scripts/mypy.exe src/taiyi`：通过，共检查二十九个源文件。
- `.venv/Scripts/pytest.exe`：通过，共二十项测试。
- Markdown 本地链接检查：通过。
- `git diff --check`：通过。

## 相关提交

- `b7d7e08dc26899cbc73c88a3fc93cd2426ec76a1`：建立只追加的迭代档案并补录历史。
- 本次回填覆盖从 `d603cd4` 到 `af9c3b0` 的七个已有提交节点。

## 影响范围

- 项目治理文档、会话交接流程、历史可追溯性和治理自动测试；不改变业务运行行为。

## 遗留事项

- 后续每次产生仓库改动的迭代都必须复制模板、追加索引并保存验证结果。
