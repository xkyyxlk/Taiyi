# 迭代-0010：提交标题规范化

## 基本信息

- 时间：2026-07-27
- 版本或里程碑：`v0.1.1` 准备
- 工作项：历史维护，没有对应正式工作项
- 状态：已完成
- 记录性质：会话迭代

## 目标

- 让每个提交标题准确概括该提交的实际差异，并安全同步已经公开的 `main` 历史。

## 本次完成内容

- 逐个审计原有九个提交的文件差异，重新拟定范围明确的中文标题。
- 保持文件树、作者和原始时间不变，重建九个线性提交。
- 更新迭代记录和索引中的旧提交哈希与标题。
- 新增提交标题约束，禁止使用没有正式工作项对应的编号。
- 使用带租约的强制推送同步远端，避免覆盖并发提交。

## 重要决策

- 提交标题采用“类型：实际改动”的中文格式，不使用项目阶段或用户意图代替差异摘要。
- 一个提交只处理一个主题；验证和影响范围进入提交正文，不挤入标题。
- 强制推送前建立本地隐藏备份引用 `refs/backup/main-before-title-rewrite-20260727`。
- 使用 `--force-with-lease` 而不是无保护的强制推送。

## 验证结果

- 重写前后 Git 文件树比较：一致。
- `.venv/Scripts/ruff.exe format --check .`：通过，共检查七十三个文件。
- `.venv/Scripts/ruff.exe check .`：通过。
- `.venv/Scripts/mypy.exe src/taiyi`：通过，共检查二十九个源文件。
- `.venv/Scripts/pytest.exe`：通过，共二十二项测试。
- Markdown 本地链接、迭代档案结构和旧提交引用检查：通过。
- 远端同步结果在带租约的强制推送后通过 `origin/main` 校验。

## 相关提交

- 本记录与同步档案的修改处于同一提交，通过本文件的 Git 历史定位。

## 影响范围

- 重写 `main` 的提交哈希和标题，更新项目治理规则与历史档案；不改变已有业务文件内容。

## 遗留事项

- 已基于旧历史创建的本地分支或克隆需要重新同步新的 `origin/main`。
