# 迭代-0022：产品原型发布决策草案

## 基本信息

- 时间：2026-07-28 10:10 +08:00
- 版本或里程碑：`v0.1.1` 产品原型发布准备
- 工作项：`TY-202607281100` 跳过真实验证并发布产品原型
- 状态：进行中
- 记录性质：会话迭代

## 目标

- 记录维护者跳过真实用户验证并直接发布当前产品原型的决定；
- 核对当前主干、版本边界、发布工具、外部凭据和完整工程检查；
- 在不编造工作项编号、不伪造签名和不越权上传的前提下形成可继续执行的发布范围。

## 本次完成内容

- 确认 `main` 与 `origin/main` 完全一致，工作树在检查前保持干净；
- 确认当前包版本仍为 `0.1.0`，原型改动位于变更日志“未发布”部分，应作为
  `v0.1.1` 发布而不是覆盖 `v0.1.0`；
- 确认仓库没有现成发布工作流，本机没有 GitHub CLI、GPG、`build` 或 `twine`；
- 确认环境中没有 GitHub 或 Python 软件包仓库发布凭据；
- 重新运行格式、静态检查、类型检查和全部测试；
- 建立“跳过真实验证并发布产品原型”待编号草案并登记索引；
- 将六个 P1 继续保留为未验证风险，不把模拟结论改写为真实用户证据。
- 维护者分配正式编号 `TY-202607281100`，并将发布目标限定为 GitHub Release；
- 创建 `codex/ty-202607281100-发布产品原型` 分支，开始 `v0.1.1` 发布准备。
- 将 `pyproject.toml` 和包内版本统一更新为 `0.1.1`，并新增版本一致性自动测试；
- 将变更日志冻结为 `0.1.1`，同步更新 README、产品定义、开发计划、已知限制和发布清单；
- 构建源码包和 wheel，检查构建内容，并在全新 Python 3.13 虚拟环境完成安装；
- 使用全新数据目录启动已安装 wheel 中的产品原型，HTTP 冒烟检查通过。
- 将发布准备提交 `2c89d13` 推送到远端功能分支，并快进合入本地 `main`；
- 在合入后的 `main` 重新运行完整工程检查；
- 新增面向 GitHub Release 的 `v0.1.1` 产品原型发布说明。
- 从最终发布提交 `bb90d4d` 重新构建并核对源码包与 wheel；
- 维护者明确接受未签名 Alpha 标签后，创建并推送注释标签 `v0.1.1`；
- 将最终发布附件复制到仓库忽略的本地 `dist/` 目录，供维护者在 Chrome 上传。

## 重要决策

- 接受维护者跳过真实用户验证的产品决策，但在发布说明中继续披露这一限制；
- 当前原型包含 `0.1.0` 之后的多项改动，因此目标版本使用 `v0.1.1`；
- 正式编号、签名配置和发布凭据未就绪前，不创建标签或上传软件包，避免不可追溯或
  无法完成的半发布状态。

## 验证结果

- `git fetch origin --prune`：通过；
- `git rev-list --left-right --count main...origin/main`：通过，结果为 `0 0`；
- `.venv/Scripts/ruff.exe format --check .`：通过，共检查九十八个文件；
- `.venv/Scripts/ruff.exe check .`：通过；
- `.venv/Scripts/mypy.exe src/taiyi`：通过，共检查三十二个源文件；
- `.venv/Scripts/pytest.exe`：通过，共二十五项测试；
- 发布工具与凭据核对：未满足，缺少签名配置、发布客户端和发布凭据。
- 版本更新后 `.venv/Scripts/ruff.exe format --check .`：通过，共检查一百个文件；
- 版本更新后 `.venv/Scripts/ruff.exe check .`：通过；
- 版本更新后 `.venv/Scripts/mypy.exe src/taiyi`：通过，共检查三十二个源文件；
- 版本更新后 `.venv/Scripts/pytest.exe`：通过，共二十六项测试；
- `.venv/Scripts/python.exe -m build`：通过，成功生成 `0.1.1` 源码包和 wheel；
- 构建产物内容检查：通过，未发现本地数据库或密钥；
- 全新 Python 3.13 虚拟环境安装和 `taiyi --help`：通过；
- 全新数据目录产品原型 HTTP 冒烟：通过，返回 HTTP 200 且页面标题内容正确；
- 合入记录前的候选安装包 SHA-256 为
  候选哈希 `EF8B4CFE48429EF2DD47403F35DD6FCD816FE481B6347FB4C3B3C91102C47B4F`；
- 合入记录前的候选源码包 SHA-256 为
  候选哈希 `77873EE54E24E74CE2955C3F808096676913F75486FA2F17C1F65AD143D7B4CA`；最终发布产物将在
  标签提交冻结后重新构建和核对。
- `git push -u origin HEAD`：通过，远端功能分支已建立；
- `git merge --ff-only codex/ty-202607281100-发布产品原型`：通过；
- 合入后完整工程检查：通过，格式、静态检查、类型检查和二十六项测试均无失败。
- 最终安装包 SHA-256：`EF8B4CFE48429EF2DD47403F35DD6FCD816FE481B6347FB4C3B3C91102C47B4F`；
- 最终源码包 SHA-256：`597D74EA94CB8088D1736D9E18EBA8764BEFB319A55F3665110EF807EB2872FB`；
- `git push origin v0.1.1`：通过，远端注释标签已建立并指向 `bb90d4d`。

## 相关提交

- `2c89d13`：`TY-202607281100 发布：准备 v0.1.1 产品原型`；
- 本次合入记录和发布说明处于后续提交，通过本记录的文件历史定位。

## 影响范围

- 新增发布决策草案和本次迭代记录；
- 更新草案索引、迭代记录索引和当前迭代状态；
- 不修改产品代码、包版本、数据库、接口、构建产物、Git 标签或外部发布状态。

## 遗留事项

- 需要完成 `0.1.1` 构建、全新安装和产品原型冒烟验证；
- 当前没有可用的标签签名配置，需要配置签名方式或由维护者接受未签名标签；
- 维护者已接受未签名标签；需要在 Chrome 创建 GitHub Release 并附加构建产物；
- 发布完成后更新本记录、发布清单和迭代状态。

## 更正说明

- 无。
