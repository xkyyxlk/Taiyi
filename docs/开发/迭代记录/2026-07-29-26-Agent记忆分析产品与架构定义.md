# 迭代-0026：Agent 记忆分析产品与架构定义

## 基本信息

- 记录编号：`迭代-0026`
- 时间：2026-07-29 14:54 +08:00
- 版本或里程碑：Agent 长期记忆分析产品与架构
- 工作项：`TY-202607281622` Agent 长期记忆分析与变更治理
- 状态：已完成
- 记录性质：会话迭代

## 目标

- 把正式工作项确定的新产品方向写入产品定义和系统架构；
- 明确当前已发布能力与正在建设能力，避免文档提前承诺未实现功能；
- 冻结新分析核心与 `v0.1.1` 身份兼容模块的领域、数据和安全边界。

## 本次完成内容

- 重写产品定义，将太一定位为本地优先、只读旁路的 Agent 长期记忆分析与变更治理工具；
- 明确首批用户、核心任务、产品形态、中性领域对象、标准场景、产品承诺和首个里程碑；
- 明确当前可安装版本仍是 `v0.1.1` 身份演化原型，新分析能力尚未发布；
- 重写系统架构，定义协议输入、分析流水线、规则、策略、报告、适配器和隐私边界；
- 新增架构决策 0006，接受版本化 JSONL、中性领域、确定性分析、默认离线和只读旁路方案；
- 保留架构决策 0002 至 0005 对身份兼容模块的适用范围，不迁移旧 SQLite 或改变身份不变量。

## 重要决策

- 新分析模块和身份兼容模块可以同包发布，但不得共享领域语义、数据库表或旧主键；
- 首个里程碑不建设常驻分析数据库，输入和报告使用用户明确指定的文件或目录；
- LangGraph/LangSmith 只作为外围参考适配器，分析核心保持框架无关；
- 确定性错误可以阻断，语义风险默认告警，模型不得成为无法解释的阻断依据；
- README 和默认入口在实际功能可用前不宣称新分析能力已经发布。

## 验证结果

- `git diff --check`：通过；
- 项目治理、Markdown 相对链接和中文规范测试：通过，共八项测试。

## 相关提交

- 当前记录与产品及架构定义处于同一提交，通过本记录的文件历史定位。

## 影响范围

- 更新产品定义和系统架构；
- 新增架构决策 0006；
- 更新迭代状态和永久迭代记录索引；
- 不修改产品代码、数据库、命令行、接口、发布版本或 `v0.1.1` 运行行为。

## 遗留事项

- 定义 JSONL 协议的精确记录类型、字段、枚举、顺序和兼容策略；
- 将中性领域模型和报告结构落实为协议及标准场景；
- 实现协议校验和兼容测试后，再进入确定性分析规则。

## 更正说明

- 无。
