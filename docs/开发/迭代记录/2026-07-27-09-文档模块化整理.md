# 迭代-0009：文档模块化整理

## 基本信息

- 时间：2026-07-27
- 版本或里程碑：`v0.1.1` 准备
- 工作项：`TY-1` 文档模块化整理
- 状态：已完成
- 记录性质：会话迭代

## 目标

- 将 `docs` 根目录中的零散文档按职责归入清晰的子模块，并更新仓库中的全部引用。

## 本次完成内容

- 建立产品、架构、开发、质量和发布五个文档模块。
- 将架构决策记录并入架构模块，将迭代历史并入开发模块。
- 新增统一文档索引，并从项目说明接入该入口。
- 更新协作约束、贡献指南、工作流、交接模板、迭代状态、测试和文档内部链接。
- 新增文档模块结构与本地链接自动检查，防止后续引用失效。

## 重要决策

- `docs` 根目录只保留 `文档索引.md`，其余文档必须进入职责明确的模块目录。
- 架构决策记录保留数字顺序和原文件名，但路径统一为 `docs/架构/决策记录/`。
- 当前断点、会话交接和永久历史均属于开发治理，统一放入 `docs/开发/`。

## 验证结果

- `.venv/Scripts/ruff.exe format --check .`：通过，共检查七十二个文件。
- `.venv/Scripts/ruff.exe check .`：通过。
- `.venv/Scripts/mypy.exe src/taiyi`：通过，共检查二十九个源文件。
- `.venv/Scripts/pytest.exe`：通过，共二十二项测试。
- 文档模块结构和 Markdown 本地链接自动检查：通过。

## 相关提交

- `62ade5e0a1c80723386c193435b34ef29a572808`：重组 `docs` 目录并更新引用与校验。

## 影响范围

- 文档目录结构、仓库内文档路径、协作入口和治理测试；不改变业务运行行为。

## 遗留事项

- 合入后应继续通过文档索引添加新资料，避免在 `docs` 根目录恢复零散文件。
