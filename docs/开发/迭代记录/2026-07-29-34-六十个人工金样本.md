# 迭代-0034：六十个人工金样本

## 基本信息

- 记录编号：`迭代-0034`
- 时间：2026-07-29 16:38 +08:00
- 版本或里程碑：Agent 记忆分析金样本
- 工作项：`TY-202607281622` Agent 长期记忆分析与变更治理
- 状态：已完成
- 记录性质：会话迭代

## 目标

- 将人工审查金样本从十四个扩展到至少六十个；
- 补齐记忆类型、作用域、事件类型、独立结构变化和多记录边界；
- 让固定输入同时校验精确变化、规则、完整摘要和退出码。

## 本次完成内容

- 新增 `SCN-015` 至 `SCN-060` 共四十六个固定 JSONL，与既有十四个合计六十个；
- 覆盖六种记忆类型、五种作用域、七种事件类型和六个独立结构字段；
- 覆盖五种作用域的新增与删除、双来源部分跨作用域、双记忆缺来源、变化稳定排序、三事件
  有效父子链和三事件循环；
- 新增人工复核清单 `golden-v1.json`，逐项保存标题、输入、精确变化、规则编号、完整摘要和
  退出码；
- 新增可重复维护脚本，场景规格直接声明标准答案，不调用分析引擎反推预期结果；
- 测试同时读取首批和扩展清单，检查六十个编号唯一、协议有效性及所有分析标准答案；
- 更新标准场景文档和迭代状态。

## 重要决策

- 保留既有 `scenarios.json` 和历史十四个夹具不变，扩展清单独立保存，避免改写已完成迭代
  的原始证据；
- 生成脚本只负责把显式人工规格机械写成静态文件，测试读取已提交夹具，不在运行时生成；
- 内容修改场景保持结构字段不变，以隔离 `content_modified`；结构场景每次只改变一个字段，
  以便错误定位和人工复核。

## 验证结果

- 扩展协议与分析引擎局部测试：通过，共二十二项测试；
- 六十个场景编号唯一，四十六个扩展输入均通过协议读取并匹配精确标准答案；
- `.venv/Scripts/ruff.exe format --check .`：通过，共检查一百三十五个文件；
- `.venv/Scripts/ruff.exe check .`：通过；
- `.venv/Scripts/mypy.exe src/taiyi`：通过，共检查四十二个源文件；
- `.venv/Scripts/pytest.exe`：通过，共七十四项测试；
- `git diff --check`、Markdown 相对链接、项目治理和中文规范检查：通过。

## 相关提交

- 当前记录与夹具、清单、生成脚本、测试和文档处于同一提交，通过本记录的文件历史定位。

## 影响范围

- 新增测试夹具、维护脚本、清单和文档；
- 不修改产品运行代码、协议、分析规则、策略、旧数据库、身份领域或命令行接口；
- 不调用模型、网络或外部记忆库。

## 遗留事项

- 建立十万事件和一万记忆变化的固定参考环境性能基线；
- 完成一万个里程碑组合案例并冻结性能预算；
- 新方向尚未达到工作项完成和合入 `main` 的条件。

## 更正说明

- 无。
