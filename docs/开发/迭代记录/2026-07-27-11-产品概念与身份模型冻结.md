# 迭代-0011：产品概念与身份模型冻结

## 基本信息

- 时间：2026-07-27 16:16 +08:00
- 版本或里程碑：`v0.1.1` 概念冻结与可靠性准备
- 工作项：`TY-202607271621`
- 状态：已完成
- 记录性质：工作项总结

## 目标

- 明确太一的产品定位、身份状态边界、目标用户、核心场景和能力承诺，并据此调整后续
  迭代方向；不在概念冻结中改变数据结构或业务行为。

## 本次完成内容

- 新增产品定义，统一一句话定位、身份状态三层模型、核心术语、用户画像和标准场景。
- 明确经历、候选记忆和可继承身份之间的审核边界。
- 明确来源谱系是工程硬保证，行为稳定性只作为评估指标，不涉及主观意识证明。
- 对照现有实现记录信念、治理规则、迁移、导入恢复和评估能力的差距。
- 同步 README、文档索引、开发计划、系统架构、评估、已知限制和变更日志。
- 将后续方向调整为 `v0.1.1` 数据可靠性、`v0.1.2` 运行可靠性、`v0.2`
  可观察与可探索以及 `v0.3` 可扩展与可协作。

## 重要决策

- 太一管理的是经过人工治理、可以由后续运行实例继承的身份状态，不是模型权重、完整
  历史或主观意识。
- v0.1 的可继承身份状态限于自我描述、已接受记忆、未决冲突和版本谱系；价值、目标、
  偏好、治理规则及独立信念工作流留待后续工作项。
- “AI 身份的版本控制系统”只作为谱系、分叉和审核的工程比喻，不建立与 Git 数据模型
  的逐项等价关系。
- 回放应使用可信记录验证或重建工程状态，不重新调用模型生成另一份结果。
- 本次没有改变架构、数据结构、身份不变量、归一协议或安全边界，因此不新增架构决策
  记录；相关文档只是明确现有约束。

## 验证结果

- 文档治理检查
  命令 `.venv/Scripts/pytest.exe tests/unit/test_project_governance.py tests/unit/test_language_policy.py`：
  通过，共七项测试。
- `.venv/Scripts/ruff.exe format --check .`：通过，共检查七十五个文件。
- `.venv/Scripts/ruff.exe check .`：通过。
- `.venv/Scripts/mypy.exe src/taiyi`：通过，共检查二十九个源文件。
- `.venv/Scripts/pytest.exe`：通过，共二十二项测试。
- `git diff --check`：通过。

## 相关提交

- 本记录与文档修改处于同一提交，通过本文件的 Git 历史定位。

## 影响范围

- 影响产品定位、术语、目标用户、架构说明、评估边界和后续路线图；不影响源代码、
  数据库结构、命令行接口或现有数据兼容性。

## 遗留事项

- 维护者已分配 `TY-202607271621`，但 GitHub 外部议题仍需补录并关联本分支。
- 产品定义审核通过后完成 `v0.1.0` 正式发布。
- 数据库迁移、完整性验证、导入恢复和回放必须分别建立后续工作项。

## 更正说明
