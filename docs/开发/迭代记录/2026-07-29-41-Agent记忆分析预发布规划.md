# 迭代-0041：Agent 记忆分析预发布规划

## 基本信息

- 记录编号：`迭代-0041`
- 时间：2026-07-29 17:37 +08:00
- 版本或里程碑：Agent 记忆分析首个预发布规划
- 工作项：尚无正式编号；本次按项目流程建立待编号草案
- 状态：已完成
- 记录性质：会话迭代

## 目标

- 在不启动真实项目影子验证的前提下，规划新分析能力的版本号、发布渠道、制品、兼容边界、
  验收门槛和失败策略；
- 将完整计划写入待编号草案，不提前创建功能分支或修改版本代码。

## 本次完成内容

- 核对当前 `v0.1.1` 包元数据、版本来源、变更日志、发布检查清单、发布说明和历史标签；
- 核对 Agent 记忆分析首个里程碑、协议 `1.0`、只读旁路和旧模块兼容边界；
- 推荐 Python 包 `0.2.0a1`、Git 标签 `v0.2.0-alpha.1` 和 GitHub Release 预发布；
- 将 PyPI 发布和 `0.2.0` 正式版推迟到真实项目验证及后续维护者决定；
- 建立包含目标、非目标、不变量、兼容影响、验收标准、发布流程、测试、依赖和风险的草案；
- 将新草案登记到工作项草案索引，并更新当前迭代状态。
- 修正草案中四行只有英文制品名或标签的表述，使其满足项目中文文档规范。

## 重要决策

- 工具包版本与 JSONL 协议、报告、规则和策略版本独立管理，包进入 `0.2` 不改变协议 `1.0`；
- 首个公开构建使用 Alpha 预发布，不把工程量化验收表述为真实项目价值；
- 首个预发布仅通过 GitHub Release 分发，避免在 PyPI 形成稳定发布预期；
- 旧身份演化模块继续兼容维护，本次不拆包、不迁移数据、不调整运行依赖。

## 验证结果

- `.venv/Scripts/ruff.exe format --check .`：通过，共检查一百五十个文件；
- `.venv/Scripts/ruff.exe check .`：通过；
- `.venv/Scripts/mypy.exe src/taiyi`：通过，共检查四十四个源文件；
- `.venv/Scripts/pytest.exe`：通过，共八十九项测试；
- `git diff --check`、Markdown 相对链接、项目治理和中文规范检查：通过。

## 相关提交

- 当前记录、草案、索引和迭代状态处于同一提交，通过本记录的文件历史定位。

## 影响范围

- 只新增发布规划和迭代文档，不修改版本号、包元数据、产品代码、协议、数据、接口或标签；
- 不创建功能分支，不启动发布或真实项目验证。

## 遗留事项

- 维护者确认建议版本、GitHub-only 渠道、正式版时机和标签签名边界；
- 维护者分配正式编号并建立外部议题后，才能启动发布实现。

## 更正说明

- 无。
