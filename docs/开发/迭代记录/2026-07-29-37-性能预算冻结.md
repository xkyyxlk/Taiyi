# 迭代-0037：性能预算冻结

## 基本信息

- 记录编号：`迭代-0037`
- 时间：2026-07-29 16:57 +08:00
- 版本或里程碑：Agent 记忆分析性能预算
- 工作项：`TY-202607281622` Agent 长期记忆分析与变更治理
- 状态：已完成
- 记录性质：会话迭代

## 目标

- 在相同参考环境复测十万事件与一万记忆变化最大档；
- 按工作项百分之二十回退边界冻结版本化绝对性能预算；
- 让命令行自动区分预算通过、预算违反和预算文件错误。

## 本次完成内容

- 在基线检查点之后两次独立复测最大档，耗时与峰值追踪内存均与初始基线一致；
- 新增性能预算模型 `1.0`，冻结参考环境、规模、输入哈希、初始基线、允许回退比例、绝对
  耗时与内存上限、报告大小和结果正确性；
- 总耗时上限冻结为 `21.537804` 秒，峰值追踪内存上限冻结为 `393706825` 字节，均未超过
  初始基线的百分之一百二十；
- 报告大小上限为 `4889503` 字节，要求一万项变化、零发现和退出码零；
- `analyze benchmark` 新增 `--budget`，同时校验环境、规模、输入哈希、性能和结果；
- 预算违反返回退出码二，预算文件读取或结构错误返回退出码一；
- 新增预算通过、预算违反、阈值约束、版本化预算文件和命令行退出码测试；
- 更新 README、性能基线、标准场景和迭代状态。

## 重要决策

- 使用最初建立的基线作为百分之二十回退计算点，复测只证明稳定性，不移动基线以放宽阈值；
- 环境不匹配作为预算违反明确报告，防止其他机器的结果被误称为参考环境预算通过；
- 性能预算同时检查确定性结果，避免通过减少分析内容获得表面性能提升；
- 调整阈值、环境或规模必须提升预算版本并记录原因，不能原地放宽 `1.0`；
- 当前实现没有改变协议、规则或安全边界，因此不新增架构决策记录。

## 验证结果

- 第一次最大档复测：17.893218 秒、328,088,984 字节，结果正确；
- 冻结预算后的最大档复测：17.891267 秒、328,091,338 字节，预算通过；
- 性能预算模型、命令行和退出码局部测试：通过；
- `.venv/Scripts/ruff.exe format --check .`：通过，共检查一百四十三个文件；
- `.venv/Scripts/ruff.exe check .`：通过；
- `.venv/Scripts/mypy.exe src/taiyi`：通过，共检查四十四个源文件；
- `.venv/Scripts/pytest.exe`：通过，共八十八项测试；
- `git diff --check`、Markdown 相对链接、项目治理和中文规范检查：通过。

## 相关提交

- 当前记录与预算模型、命令行、预算文件、文档和测试处于同一提交，通过本记录的文件历史
  定位。

## 影响范围

- 扩展性能模块与基准命令，新增版本化预算文件；
- 不改变协议 `1.0`、分析规则、报告结构、旧数据库或身份领域；
- 默认路径不调用模型、网络或外部记忆库。

## 遗留事项

- 汇总量化退出标准并完成工作项验收；
- 创建工作项总结并核对完整工程检查；
- 根据验收结果判断是否建立真实项目影子验证草案。

## 更正说明

- 无。
