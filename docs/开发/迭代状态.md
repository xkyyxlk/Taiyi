# 太一迭代状态

> 本文件是新会话恢复工作的第一入口。每次开始、暂停或完成工作时都要更新。

## 当前基线

- 当前版本：`v0.1.0`
- 当前里程碑：`v0.1.1` 产品原型反馈准备
- 当前分支：`main`
- 当前工作项：`TY-202607271832` 产品原型重生后世界线展示修正
- 工作状态：已合入
- 更新时间：2026-07-27 18:49 +08:00

## 最近完成

- 完成 v0.1 身份分叉、世界线、记忆、归一、快照、回滚和导出闭环；
- 完成中文文档、中文文件名和中文注释约束；
- 建立持续迭代、新会话恢复和开源协作工作流。
- 建立只追加的迭代历史档案，并回填项目此前七次迭代记录。
- 将文档归入产品、架构、开发、质量和发布模块，并建立统一索引。
- 统一既有提交标题，并同步重写后的提交哈希与迭代档案。
- 完成 `TY-202607271621` 产品概念与身份模型冻结，统一产品定义、用户画像、概念边界和
  后续迭代方向。
- 根据审核反馈完成产品阶段纠偏，明确技术原型之后先建设产品原型，再由反馈决定长期
  产品方向。
- 将 `codex/ty-202607271621-concept-model` 快进合入本地 `main`。
- 建立待编号工作项草案机制，避免完整计划只存在于聊天记录中。
- 将“产品原型核心流程与交互设计”草案转为正式工作项 `TY-202607271741`。
- 实现只监听本机回环地址的交互式产品原型，覆盖身份创建、同位体派生、经历、记忆来源、
  世界线比较、人工归一、快照、回滚和重生。
- 提供默认离线的“未知任务的两种选择”演示预设、步骤引导和响应式界面。
- 增加产品原型应用与 HTTP 集成测试、使用指南和本地界面架构决策记录。
- 将 `codex/ty-202607271741-产品原型核心流程` 快进合入本地 `main`。
- 使用全新数据目录完成创建者核心流程走查，验证来源下钻、人工审核门、快照生成、重生与
  回滚，并确认浏览器控制台没有错误；
- 将重生后“3/2 已就绪”和共享起点文案与真实谱系不一致的问题记录为待编号草案。
- 维护者为展示修正草案分配正式编号 `TY-202607271832`，并创建对应功能分支。
- 完成比较组与下一代同位体分组、准确派生计数和真实基础快照展示；
- 补充第三个同位体谱系与静态页面文案测试，并再次通过完整浏览器核心流程。
- 推送功能分支并将三个工作项提交快进合入 `main`；
- 合入后的完整工程检查通过，并将 `main` 推送到远端。

## 当前现场

- 产品定义、用户画像和路线图已经合入 `main`；
- 没有已知阻塞；
- `TY-202607271741` 已具有完整范围、验收标准、不变量、兼容影响和测试计划；
- 产品原型代码、测试、文档和架构决策已经合入本地 `main`；
- 产品原型复用现有应用服务和 SQLite 数据，没有修改领域模型或数据库结构；
- 页面资源已进入构建产物，默认服务只绑定 `127.0.0.1`；
- 功能分支已经推送并保留；
- 合入后完整检查通过，`origin/main` 已包含提交 `6e07bea` 及之前的产品原型历史；
- 创建者走查使用系统临时数据目录，没有接触现有产品数据；
- 展示修正草案已经转正为 `TY-202607271832`，范围、不变量、兼容影响和测试计划保持不变；
- 功能分支包含转正规划提交 `9a0cd7c`、实现提交 `dce4ada` 和归档提交 `5ce4fc4`；
- 前两个同位体保持为既有比较组，重生后的协调者单独显示为下一代；
- 实现没有修改领域模型、数据库、内部 JSON、命令行接口或导出格式；
- 功能分支已经推送并快进合入本地 `main`；
- 合入后完整检查通过，`origin/main` 已包含工作项提交和本次合入记录；
- 远端功能分支暂时保留，本次合入记录提交后工作树应保持干净。

## 下一步操作

1. 补录 `TY-202607271832` 的外部正式议题；
2. 邀请少量早期探索者验证比较组与下一代分组是否足够直观；
3. 根据多次真实反馈决定下一项概念简化或功能工作；
4. 维护者确认不再需要审核历史后，再决定是否删除远端功能分支。

## 最近验证

- `node --check src/taiyi/prototype/static/app.js`：通过；
- `.venv/Scripts/ruff.exe format --check .`：通过，共检查九十个文件；
- `.venv/Scripts/ruff.exe check .`：通过；
- `.venv/Scripts/mypy.exe src/taiyi`：通过，共检查三十二个源文件；
- `.venv/Scripts/pytest.exe`：通过，共二十五项测试；
- `git diff --check` 和新增、修改文档的相对链接检查：通过；
- 全新数据目录浏览器复核：创建、分叉、经历、来源、比较、审核、应用、重生和回滚通过；
- 重生和回滚后的分组、数量与基础快照均正确，浏览器控制台没有错误或警告。
- `git push -u origin HEAD`：通过，远端功能分支已建立；
- `git merge --ff-only codex/ty-202607271832-产品原型世界线展示`：通过；
- 合入后完整工程检查：通过，共二十五项测试；
- `git push origin main`：通过。

## 阻塞与待决定事项

- 当前原型可以运行，没有运行故障阻塞；
- 尚未进行真实早期用户反馈收集，下一阶段方向不得仅凭本次实现决定；
- `TY-202607271832` 的外部正式议题仍待补录，但维护者已经明确分配编号并授权实现；
- `TY-202607271621` 的外部 GitHub 议题仍待补录，但不影响本地合入结果。

## 相关入口

- [项目迭代工作流](项目迭代工作流.md)
- [工作项草案索引](工作项草案/草案索引.md)
- [产品原型核心流程与交互设计](工作项草案/2026-07-27-产品原型核心流程与交互设计.md)
- [产品原型重生后世界线展示修正](工作项草案/2026-07-27-产品原型重生后世界线展示修正.md)
- [会话交接模板](会话交接模板.md)
- [迭代记录索引](迭代记录/记录索引.md)
- [最新迭代记录](迭代记录/2026-07-27-19-世界线展示修正分支合入.md)
- [文档索引](../文档索引.md)
- [产品定义](../产品/产品定义.md)
- [产品原型使用指南](../产品/产品原型使用指南.md)
- [v0.1 开发计划](../产品/开发计划_v0.1.md)
- [已知限制](../产品/已知限制.md)
- [本地产品原型界面决策](../架构/决策记录/0005-本地产品原型界面.md)
- [架构决策记录](../架构/决策记录/)
