# 太一迭代状态

> 本文件是新会话恢复工作的第一入口。每次开始、暂停或完成工作时都要更新。

## 当前基线

- 当前版本：`v0.1.1`
- 当前里程碑：Agent 长期记忆分析核心能力建设
- 当前分支：`codex/ty-202607281622-agent记忆治理`
- 当前工作项：`TY-202607281622` Agent 长期记忆分析与变更治理
- 工作状态：开发中；性能预算已冻结，进入工作项最终量化验收
- 更新时间：2026-07-29 16:57 +08:00

## 最近完成

- 完成 v0.1 身份分叉、世界线、记忆、归一、快照、回滚和导出闭环；
- 完成中文文档、中文文件名和中文注释约束；
- 建立持续迭代、新会话恢复和开源协作工作流。
- 建立只追加的迭代历史档案，并回填项目此前七次迭代记录。
- 将文档归入产品、架构、开发、质量和发布模块，并建立统一索引。
- 统一既有提交标题，并同步重写后的提交哈希与迭代档案。
- 完成 `TY-202607271621` 产品概念与身份模型冻结，统一产品定义、用户画像、概念边界和
  后续迭代方向。
- 根据审核反馈完成产品阶段纠偏，明确技术原型之后先建设产品原型，再由反馈决定长期
  产品方向。
- 将 `codex/ty-202607271621-concept-model` 快进合入本地 `main`。
- 建立待编号工作项草案机制，避免完整计划只存在于聊天记录中。
- 将“产品原型核心流程与交互设计”草案转为正式工作项 `TY-202607271741`。
- 实现只监听本机回环地址的交互式产品原型，覆盖身份创建、同位体派生、经历、记忆来源、
  世界线比较、人工归一、快照、回滚和重生。
- 提供默认离线的“未知任务的两种选择”演示预设、步骤引导和响应式界面。
- 增加产品原型应用与 HTTP 集成测试、使用指南和本地界面架构决策记录。
- 将 `codex/ty-202607271741-产品原型核心流程` 快进合入本地 `main`。
- 使用全新数据目录完成创建者核心流程走查，验证来源下钻、人工审核门、快照生成、重生与
  回滚，并确认浏览器控制台没有错误；
- 将重生后“3/2 已就绪”和共享起点文案与真实谱系不一致的问题记录为待编号草案。
- 维护者为展示修正草案分配正式编号 `TY-202607271832`，并创建对应功能分支。
- 完成比较组与下一代同位体分组、准确派生计数和真实基础快照展示；
- 补充第三个同位体谱系与静态页面文案测试，并再次通过完整浏览器核心流程。
- 推送功能分支并将三个工作项提交快进合入 `main`；
- 合入后的完整工程检查通过，并将 `main` 推送到远端。
- 补录并关闭 `TY-202607271621` 和 `TY-202607271832` 的外部正式议题；
- 为 `TY-202307271905` 建立开放的外部议题、完整范围文档和功能分支。
- 建立覆盖五类场景、四类角色和三轮十五次模拟的探索场景矩阵；
- 建立统一模拟记录格式，完成十五次角色推演并汇总真实用户待验证问题；
- 明确模拟没有发现 P0 问题，且不能替代真实用户反馈或直接授权产品修改。
- 推送探索场景模拟功能分支并将四个工作项提交快进合入 `main`；
- 合入后的完整工程检查通过，GitHub 议题 #3 已按完成关闭；
- 将合入记录提交并推送到远端 `main`。
- 完成 `TY-202607281100` 发布准备、最终构建、标签、GitHub Release 和正式议题归档；
- `v0.1.1` 产品原型已公开发布，源码包与 wheel 均可从发布页下载；
- GitHub 议题 #4 已补建并按完成关闭。
- 完成现有产品方向压力测试，将候选新方向收敛为面向 Agent 开发者与研究型开发者的
  长期记忆分析与变更治理工具；
- 确认只读 CLI/CI、框架无关协议、LangGraph/LangSmith 参考适配器、中性领域模型、
  确定性分析和大规模模拟优先等边界；
- 建立待编号草案，不创建功能分支，不修改产品代码或现有产品定义。
- 维护者为新方向分配正式编号 `TY-202607281622`，原草案已追加转正信息；
- 正式议题尚待建立，功能分支和产品实现仍未启动。
- 建立正式议题 [GitHub #5](https://github.com/xkyyxlk/Taiyi/issues/5)，并创建功能分支
  `codex/ty-202607281622-agent记忆治理`。
- 更新产品定义和系统架构，将 Agent 长期记忆分析确定为新方向，并把 `v0.1.1` 身份演化
  原型限定为独立兼容模块；
- 新增架构决策 0006，冻结只读旁路、版本化协议、中性领域、确定性规则、默认离线和新旧
  数据隔离边界。
- 定义 Agent 记忆分析 JSONL 协议 `1.0`，明确记录类型、字段、顺序、版本、哈希和兼容策略；
- 新增独立 `taiyi.analysis` 协议模型与严格读取器，校验重复字段、未知字段、记录顺序、
  快照归属、哈希、重复标识和悬空引用；
- 建立十二类标准场景清单和首批四个机器夹具，协议单元测试共十四项通过。
- 定义变化、发现、策略和报告 `1.0` 结构，冻结四个首批规则编号及零、一、二退出码语义；
- 实现记忆新增、删除、内容修改和结构变化，以及来源缺失、作用域不一致、父子时间倒置和
  事件循环规则；
- 实现版本化策略覆盖、确定性排序、输入哈希、摘要和结构化报告，标准场景开始直接校验
  规则编号与退出码。
- 增加 `analyze validate` 和 `analyze check`，支持版本化策略文件、JSON 报告及稳定的
  零、一、二退出码；
- 分析命令绕过旧运行时初始化，不创建旧 SQLite、不加载模型提供器，也不写入外部记忆库；
- 报告增加参数数组形式的复现命令，并补充命令行、策略降级和错误路径集成测试。
- 实现确定性的 JSON、Markdown 和 HTML 报告渲染；
- `analyze check` 增加 `--format` 与 `--output`，支持创建父目录并使用 UTF-8 写入报告；
- 验证人类可读报告不复制原始事件或记忆正文，HTML 会转义动态标识以防止页面结构注入。
- 将手工审查机器夹具扩展到十四个，覆盖新增、删除、结构变化、协议错误和两类审计链风险；
- 实现固定种子组合生成器与 `analyze simulate`，一千个提交级案例可重复生成并与独立标准
  答案比较；
- 组合模拟不调用模型或网络，不初始化旧身份 SQLite，并输出逐案例 JSONL 和版本化清单。
- 冻结 LangGraph `1.2.10`、LangSmith `0.10.11` 和适配器契约 `1.0` 离线参考版本；
- 实现 Store 快照、LangSmith 运行与显式写入旁车记录到中性 JSONL 的确定性转换；
- 增加 `analyze adapt-langgraph`、离线金夹具和显式写入辅助函数，核心运行依赖未增加框架
  SDK，转换路径不复制原始 Store 值或运行输入输出。

## 当前现场

- 产品定义、用户画像和路线图已经合入 `main`；
- 没有已知阻塞；
- `TY-202607271741` 已具有完整范围、验收标准、不变量、兼容影响和测试计划；
- 产品原型代码、测试、文档和架构决策已经合入本地 `main`；
- 产品原型复用现有应用服务和 SQLite 数据，没有修改领域模型或数据库结构；
- 页面资源已进入构建产物，默认服务只绑定 `127.0.0.1`；
- 功能分支已经推送并保留；
- 合入后完整检查通过，`origin/main` 已包含提交 `6e07bea` 及之前的产品原型历史；
- 创建者走查使用系统临时数据目录，没有接触现有产品数据；
- 展示修正草案已经转正为 `TY-202607271832`，范围、不变量、兼容影响和测试计划保持不变；
- 功能分支包含转正规划提交 `9a0cd7c`、实现提交 `dce4ada` 和归档提交 `5ce4fc4`；
- 前两个同位体保持为既有比较组，重生后的协调者单独显示为下一代；
- 实现没有修改领域模型、数据库、内部 JSON、命令行接口或导出格式；
- 功能分支已经推送并快进合入本地 `main`；
- 合入后完整检查通过，`origin/main` 已包含工作项提交和本次合入记录；
- 远端功能分支暂时保留，本次合入记录提交后工作树应保持干净。
- GitHub 议题 [#1](https://github.com/xkyyxlk/Taiyi/issues/1) 和
  [#2](https://github.com/xkyyxlk/Taiyi/issues/2) 已按历史完成状态关闭；
- GitHub 议题 [#3](https://github.com/xkyyxlk/Taiyi/issues/3) 已按完成关闭，本工作项只产生
  探索与模拟文档，没有修改产品代码；
- `main` 已包含工作项提交 `b806b8c`、矩阵与模板提交 `1fe7d4a`、模拟记录提交
  `7769c0f` 和归档提交 `aed8ea5`；
- 三轮共十五次模拟覆盖 S1 至 S5 和 R1 至 R4，六个反复出现的 P1 问题均标记为
  真实用户待验证；
- 功能分支已推送并保留，`main` 已快进合入且完整检查通过；
- 当前没有产品代码、数据结构、接口或运行行为改动，工作树应保持干净。
- `v0.1.1` 未签名注释标签指向发布提交 `bb90d4d`，维护者已明确接受该 Alpha 发布边界；
- [GitHub Release](https://github.com/xkyyxlk/Taiyi/releases/tag/v0.1.1) 已于
  2026-07-28 11:59:58 +08:00 发布，不是草稿或预发布版本；
- Release 包含 `taiyi_ai-0.1.1.tar.gz` 和 `taiyi_ai-0.1.1-py3-none-any.whl` 两个附件；
- 正式工作项议题 [#4](https://github.com/xkyyxlk/Taiyi/issues/4) 已按完成关闭；
- 本次发布没有修改数据库结构、领域模型、接口或身份安全不变量。
- 新方向草案已转正为 `TY-202607281622`，但尚未替换 `v0.1.1` 的正式产品定义；
- 旧身份演化原型拟进入兼容维护，新方向拟使用独立中性领域模型和独立存储；
- 草案建议单维护者用 4–6 周完成版本化协议、确定性分析、CI、参考适配器和分层模拟；
- 正式议题与功能分支已经建立，产品代码、数据库、接口和发布版本仍未修改；
- 产品与架构定义、协议 `1.0`、结构校验、确定性差异、首批规则、策略和 JSON 报告已经完成；
- 新分析命名空间不依赖旧身份领域、SQLite、模型提供器或本地产品原型；
- 分析 CLI、策略读取、JSON 输出和持续集成退出码已经完成；
- JSON、Markdown 和 HTML 报告及显式输出路径已经完成；
- 十四个机器夹具、固定种子提交级组合生成器和模拟命令已经完成；
- LangGraph/LangSmith 离线参考适配器和显式写入旁车记录已经完成；
- 固定种子畸形输入生成器和 `analyze simulate-malformed` 已完成，十四类错误均保存稳定标准
  答案，一千个提交级案例可重复运行；
- 人工审查金样本已从十四个扩展到六十个，新增记忆类型、作用域、事件类型、独立结构字段、
  多来源、多记忆排序和三事件链路覆盖；
- 固定参考环境性能基线已覆盖一千、一万和十万事件档，最大档包含一万项记忆变化；
- 里程碑生成器已覆盖多记忆、多来源、全部作用域、可选正文和版本组合，一万案例零差异；
- 参考环境性能预算 `1.0` 已冻结，最大档复测通过绝对耗时、峰值追踪内存、报告大小和结果
  正确性检查；
- 工作项最终量化验收与总结仍未完成。

## 下一步操作

1. 汇总量化退出标准并完成工作项验收；
2. 创建工作项总结并核对完整工程检查；
3. 根据验收结果判断是否建立真实项目影子验证草案。

## 未提交现场

- 性能预算模型、校验命令、预算文件、文档、测试、状态和迭代记录正在形成当前检查点提交。

## 最近验证

- `node --check src/taiyi/prototype/static/app.js`：通过；
- `.venv/Scripts/ruff.exe format --check .`：通过，共检查九十个文件；
- `.venv/Scripts/ruff.exe check .`：通过；
- `.venv/Scripts/mypy.exe src/taiyi`：通过，共检查三十二个源文件；
- `.venv/Scripts/pytest.exe`：通过，共二十五项测试；
- `git diff --check` 和新增、修改文档的相对链接检查：通过；
- 全新数据目录浏览器复核：创建、分叉、经历、来源、比较、审核、应用、重生和回滚通过；
- 重生和回滚后的分组、数量与基础快照均正确，浏览器控制台没有错误或警告。
- `git push -u origin HEAD`：通过，远端功能分支已建立；
- `git merge --ff-only codex/ty-202607271832-产品原型世界线展示`：通过；
- 合入后完整工程检查：通过，共二十五项测试；
- `git push origin main`：通过。
- `v0.1.1` 发布候选完整工程检查：通过，格式、静态检查、类型检查和二十六项测试均无失败；
- `.venv/Scripts/python.exe -m build`：通过，生成 `taiyi_ai-0.1.1.tar.gz`，对应安装包为
  安装包文件 `taiyi_ai-0.1.1-py3-none-any.whl`；
- 构建产物内容与元数据检查：通过，wheel 包含产品原型静态资源，版本为 `0.1.1`，未发现
  本地数据库或密钥；
- 全新 Python 3.13 虚拟环境安装 wheel：通过，`taiyi.__version__` 为 `0.1.1`，
  `taiyi --help` 正常；
- 全新数据目录产品原型冒烟：通过，`127.0.0.1:18765` 返回 HTTP 200 且页面包含“太一”。
- `git push -u origin HEAD`：通过，远端发布功能分支已建立；
- `git merge --ff-only codex/ty-202607281100-发布产品原型`：通过；
- 合入 `main` 后完整工程检查：通过，共二十六项测试。
- 最终发布提交 `bb90d4d` 的完整工程检查和重新构建：通过；
- 最终安装包 SHA-256：`EF8B4CFE48429EF2DD47403F35DD6FCD816FE481B6347FB4C3B3C91102C47B4F`；
- 最终源码包 SHA-256：`597D74EA94CB8088D1736D9E18EBA8764BEFB319A55F3665110EF807EB2872FB`；
- 未签名注释标签 `v0.1.1`：经维护者明确接受，已创建并推送到 `origin`。
- GitHub Release 核对：通过，`v0.1.1` 已发布且不是草稿或预发布版本；
- Release 附件核对：通过，源码包 119907 字节，wheel 52334 字节；
- GitHub 议题 #4：已补建范围与完成证据，并以 `completed` 原因关闭。
- 最终发布归档完整工程检查：通过，格式、静态检查、类型检查和二十六项测试均无失败。
- 模拟记录完整性检查：通过，共十五条，覆盖五类场景和四类角色；
- 项目治理与中文规范测试 `.venv/Scripts/pytest.exe tests/unit/test_project_governance.py tests/unit/test_language_policy.py`：
  通过，共七项测试；
- 本工作项完整工程检查：通过，格式、静态检查、类型检查和全部测试均无失败。
- `git push -u origin HEAD`：通过，探索场景模拟功能分支已建立；
- `git merge --ff-only codex/ty-202307271905-探索场景模拟`：通过；
- 合入后完整工程检查：通过，共二十五项测试；
- GitHub 议题 #3：已按完成关闭；
- `git push origin main`：通过。
- Agent 长期记忆分析方向草案的 `git diff --check` 和相对链接检查：通过；
- 项目治理与中文规范测试：通过。
- `TY-202607281622` 草案转正文档的 `git diff --check`、相对链接和治理检查：通过。
- Agent 记忆分析产品与架构文档的 `git diff --check`：通过；
- 项目治理、Markdown 相对链接和中文规范测试：通过，共八项测试。
- Agent 记忆分析协议单元测试：通过，共十四项测试；
- 协议实现局部格式、静态检查和类型检查：通过；
- 本次协议检查点完整工程检查：通过，格式、静态检查、类型检查和全部测试均无失败。
- 确定性分析相关测试：通过，共二十二项分析与协议测试；
- 本次分析检查点完整工程检查：通过，格式、静态检查、类型检查和全部测试均无失败。
- 分析命令行、协议与规则相关测试：通过，共二十八项测试；
- 本次命令行检查点完整工程检查：通过，格式、静态检查、类型检查和全部测试均无失败。
- 报告渲染、命令行、协议与规则相关测试：通过，共三十三项测试；
- 本次报告检查点完整工程检查：通过，格式、静态检查、类型检查和全部测试均无失败。
- 固定种子组合、协议、规则和命令行局部测试：通过，共三十五项测试；
- 本次组合模拟检查点完整工程检查：通过，共一百二十六个格式文件、三十九个源文件和
  六十三项测试，静态检查、类型检查、相对链接、项目治理及中文规范均无失败。
- 参考适配器、CLI、链接和中文规范局部测试：通过，共二十四项测试；
- 本次参考适配器检查点完整工程检查：通过，共一百三十一个格式文件、四十一个源文件和
  七十项测试，静态检查、类型检查、相对链接、项目治理及中文规范均无失败。
- 固定种子畸形输入局部测试：通过，一千个案例覆盖十四类错误且与稳定标准答案零差异。
- 本次畸形输入检查点完整工程检查：通过，共一百三十三个格式文件、四十二个源文件和
  七十四项测试，静态检查、类型检查、相对链接、项目治理及中文规范均无失败。
- 六十个金样本清单审计：通过，场景编号唯一，四十六个扩展文件齐全且精确标准答案零差异；
- 本次金样本检查点完整工程检查：通过，共一百三十五个格式文件、四十二个源文件和
  七十四项测试，静态检查、类型检查、相对链接、项目治理及中文规范均无失败。
- 固定参考环境三档性能采样：通过，最大档包含十万事件和一万记忆变化，实际变化数正确且
  没有治理发现；
- 本次性能基线检查点完整工程检查：通过，共一百三十九个格式文件、四十三个源文件和
  八十项测试，静态检查、类型检查、相对链接、项目治理及中文规范均无失败。
- 一万里程碑组合案例：通过，覆盖 5,040 个不同维度，套件哈希稳定且零标准答案差异；
- 本次里程碑模拟检查点完整工程检查：通过，共一百四十二个格式文件、四十四个源文件和
  八十四项测试，静态检查、类型检查、相对链接、项目治理及中文规范均无失败。
- 冻结预算后的最大档复测：通过，总耗时 17.891267 秒，峰值追踪内存 328,091,338 字节，
  输入哈希、变化数、发现数、报告大小和退出码均匹配预算；
- 本次性能预算检查点完整工程检查：通过，共一百四十三个格式文件、四十四个源文件和
  八十八项测试，静态检查、类型检查、相对链接、项目治理及中文规范均无失败。

## 阻塞与待决定事项

- 当前原型可以运行，没有运行故障阻塞；
- 维护者已决定跳过真实早期用户反馈收集并直接发布当前原型；
- 角色模拟仍不能替代真实用户证据，六个 P1 不得标记为已验证或已解决；
- 正式工作项编号为 `TY-202607281100`，发布目标仅为 GitHub Release；
- 维护者已经接受本次 Alpha 原型使用未签名注释标签；
- GitHub Release、附件、标签和正式议题均已完成，没有发布阻塞；
- 是否在发布后继续验证候选记忆、可信度、策略、回滚或重生说明，由后续工作项决定。
- 新方向已经建立正式工作项、外部议题和功能分支，没有流程阻塞；
- 大规模模拟只验证工程正确性，真实项目验证推迟到量化退出标准满足后，不能将模拟结果
  表述为实际采用价值或市场证据。

## 相关入口

- [项目迭代工作流](项目迭代工作流.md)
- [工作项草案索引](工作项草案/草案索引.md)
- [Agent 长期记忆分析与变更治理草案](工作项草案/2026-07-28-Agent长期记忆分析与变更治理.md)
- [产品原型核心流程与交互设计](工作项草案/2026-07-27-产品原型核心流程与交互设计.md)
- [产品原型重生后世界线展示修正](工作项草案/2026-07-27-产品原型重生后世界线展示修正.md)
- [产品原型探索场景与模拟反馈](工作项草案/2026-07-27-产品原型探索场景与模拟反馈.md)
- [跳过真实验证并发布产品原型](工作项草案/2026-07-28-跳过真实验证并发布产品原型.md)
- [会话交接模板](会话交接模板.md)
- [迭代记录索引](迭代记录/记录索引.md)
- [最新迭代记录](迭代记录/2026-07-29-37-性能预算冻结.md)
- [Agent 记忆分析性能基线](../质量/Agent记忆分析性能基线_v1.md)
- [产品原型探索场景矩阵](../产品/探索场景/场景矩阵.md)
- [产品原型多轮模拟记录](../产品/探索场景/2026-07-27-多轮模拟记录.md)
- [文档索引](../文档索引.md)
- [产品定义](../产品/产品定义.md)
- [产品原型使用指南](../产品/产品原型使用指南.md)
- [v0.1 开发计划](../产品/开发计划_v0.1.md)
- [已知限制](../产品/已知限制.md)
- [v0.1.1 产品原型发布说明](../发布/v0.1.1-产品原型发布说明.md)
- [v0.1.1 产品原型 GitHub Release 发布页](https://github.com/xkyyxlk/Taiyi/releases/tag/v0.1.1)
- [TY-202607281100 正式议题](https://github.com/xkyyxlk/Taiyi/issues/4)
- [TY-202607281622 正式议题](https://github.com/xkyyxlk/Taiyi/issues/5)
- [本地产品原型界面决策](../架构/决策记录/0005-本地产品原型界面.md)
- [Agent 长期记忆分析旁路架构决策](../架构/决策记录/0006-Agent长期记忆分析旁路架构.md)
- [Agent 记忆分析协议 v1](../架构/Agent记忆分析协议_v1.md)
- [Agent 记忆分析规则与报告 v1](../架构/Agent记忆分析规则与报告_v1.md)
- [LangGraph 与 LangSmith 参考适配器 v1](../架构/LangGraph与LangSmith参考适配器_v1.md)
- [Agent 记忆分析标准场景](../质量/Agent记忆分析标准场景.md)
- [架构决策记录](../架构/决策记录/)
