# 项目协作约束

## 开始工作前

- 先阅读 `README.md`、`docs/文档索引.md`、`docs/开发/项目迭代工作流.md`、`docs/开发/迭代状态.md` 和 `docs/开发/迭代记录/记录索引.md`。
- 根据当前工作项阅读索引中的最近相关迭代记录，确认历史决策、验证证据和遗留事项。
- 再阅读当前工作项引用的议题、架构决策记录和相关设计文档。
- 执行 `git status --short --branch` 和 `git log -5 --oneline`，核对实际分支、未提交改动和最近提交。
- 从 `docs/开发/迭代状态.md` 的“下一步操作”继续，不得重复已经完成的工作。
- 如果状态文档与 Git 现场不一致，以保护现场为先；不得重置或覆盖改动，应先查明差异并更新状态文档。

## 迭代与会话交接

- 每项工作使用唯一编号，格式为 `TY-<数字>`，并在提交、分支和合并请求中引用。
- 只有已经正式建立并可追溯的工作项才能使用 `TY-<数字>`；不得自行编造编号。
- 尚未取得正式编号但已经形成完整范围的想法，必须记录在
  `docs/开发/工作项草案/`，不得只保留在聊天中。草案不是正式工作项，不使用临时
  `TY-<数字>`，也不授权创建功能分支或开始实现。
- 草案取得正式编号后，在原草案末尾追加转正信息并关联正式议题；不得删除草案或用正式
  工作项覆盖其历史。
- Codex 开发分支采用 `codex/ty-<编号>-<主题>`；不要直接在 `main` 上开始未确定范围的功能开发。
- 提交标题必须使用中文准确概括暂存差异，一个提交只处理一个主题；提交前必须用 `git diff --cached` 核对标题与内容，验证和影响范围写入提交正文。
- 架构、数据结构、身份不变量、归一协议或安全边界发生变化时，必须新增或更新 `docs/架构/决策记录/` 下的架构决策记录。
- 每完成一个可验证的小步骤就运行相关检查；不要把测试、文档和迁移集中拖到迭代末尾。
- 会话结束或准备切换新会话前，必须按 `docs/开发/会话交接模板.md` 更新 `docs/开发/迭代状态.md`。
- 每次产生仓库改动的开发迭代都必须在 `docs/开发/迭代记录/` 新建记录并追加索引；跨会话工作项保留每次会话记录，完成时再增加工作项总结。
- 已完成迭代记录只允许追加更正说明，不得删除、改写或用当前状态、提交信息和变更日志替代。
- 未完成工作优先提交到功能分支形成检查点；不得把仅存在于聊天记录中的信息视为项目状态。

## 中文规范

- 本项目新增或修改的文档必须使用中文，包括 Markdown、发布说明、议题模板和合并请求模板。
- 源代码中的注释和文档字符串必须使用中文；仅在解释设计原因、边界条件或不明显行为时添加注释。
- 命令、代码标识符、字段名、文件路径、协议名、产品名和其他无法准确翻译的专有名词可以保留原文。
- 第三方代码、自动生成文件、法律许可证原文和机器识别指令不受中文规范约束，不得为了翻译而破坏其语义或有效性。

## 验证要求

- 修改文档后检查相对链接和文件名是否有效。
- 修改代码注释或文档字符串后运行相关格式、静态检查和测试。
- 常规完整检查为 `ruff format --check .`、`ruff check .`、`mypy src/taiyi` 和 `pytest`。
- 只有在验收标准满足、测试通过、文档、迭代记录与 `docs/开发/迭代状态.md` 已更新后，工作项才算完成。
